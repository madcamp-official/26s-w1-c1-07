# 16:1354 — 온라인 게임하기 (코드 생성 후)

- 이미지: `design-lab/reference/figma/16-1354__온라인_게임하기.png`
- 캔버스 위치: (-3657, 4916) — 16:1321 바로 오른쪽 옆 (상태 시퀀스 배치)

## 역할 추정
16:1321과 동일한 온라인 매칭/방 관리 패널의 **"코드 생성하기"를 누른 직후 상태**. 생성된 방 코드가 표시되어 있고, 방장은 이 코드를 상대에게 전달한 뒤 입장을 기다린다.

## 구성요소 (위치 포함)
16:1321과 완전히 동일하되 한 곳만 다름:
- **배경**: 우상단 "SIGN IN WITH GOOGLE", 원형 "설정" 버튼, 하단 "오프라인 게임하기" 버튼 일부
- **중앙 대형 패널**:
  - 제목: "온라인 게임하기"
  - 흰색 버튼: "빠른 시작"
  - 진회색 섹션 "게임 만들기 / 참가하기":
    - 1행: "코드 생성하기" 버튼 + **생성된 코드 텍스트 "34823501249"** (16:1321에서는 빈 밑줄) + "복사" 버튼 + 톱니 아이콘
    - 2행: "코드 입력하기" 라벨 + 입력창 + "확인" 버튼

## 기능 목록
(16:1321과 동일 세트 — 상태만 다름)
1. **생성된 코드 표시**
   - (a) 방 코드(예: 34823501249, 11자리 숫자)를 화면에 노출.
   - (b) 트리거: "코드 생성하기" 클릭 결과.
   - (c) mock: 상태값 `roomCode`를 렌더. 재클릭 시 재생성 여부는 정책 결정 필요.
2. **코드 복사** — 이 상태에서 실제로 활성화되는 기능. 클립보드 복사 + 피드백.
3. **상대 입장 대기** (암시)
   - (a) 코드 생성 후 상대가 코드를 입력해 들어올 때까지 대기. 와이어프레임에 별도 대기 UI는 없음.
   - (b) 트리거: 코드 생성 완료 시점부터.
   - (c) mock: n초 후 가짜 상대 입장 이벤트 발생 → 게임 시작 화면(16:1387 계열)으로 전환.
4. **빠른 시작 / 코드 입력 참가 / 방 설정(톱니)** — 16:1321 문서와 동일.

## 특이사항
- **16:1321의 상태 변형** — 유일한 차이는 코드 자리(빈 밑줄 → "34823501249"). 캔버스에서도 같은 y좌표(4916)에 좌우로 나란히 놓여 있어 before/after 시퀀스임이 명확.
- 코드가 11자리 숫자로 그려져 있으나 실사용엔 과하게 김 — 구현 시 4~6자리로 줄이는 것을 검토할 만함(와이어프레임은 형식 예시일 뿐).
- 상대 입장 후의 화면(대기실/카운트다운)은 이 담당 프레임엔 없음 — 16:1387 "온라인 게임하기" 프레임(타 담당) 분석과 연결 필요.
