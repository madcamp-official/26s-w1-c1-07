# 17:1206 — 게임2 - P2

PNG: design-lab/reference/figma/17-1206__게임2_-_P2.png

## 역할 추정
게임2 P2(수비/회피) 시점 인게임 화면. **16:1515 프레임과 완전히 동일한 복제본** (export 파일 크기·픽셀 동일). 다른 페이지/버전에 복사된 것.

## 구성요소 (위치와 함께) — 16:1515와 동일
- 상단 가로선 + 중앙 원형 배지 "P1" (상대 위치)
- 낙하 중인 노트 3개 (빨간 반원 캡슐, P1 쪽에서 아래로)
- 중앙의 흐린 원형 패드 2개: ← "P", → "Q"
- 하단 가로선 + 우측 원형 배지 "P2" (내 위치)

## 기능 목록 — 16:1515 분석과 동일
1. 상대(P1) 좌우 위치 실시간 표시 (mock: 가짜 이동 시뮬레이션)
2. 노트 낙하 렌더링 (mock: rAF 루프 + mock 발사 큐)
3. P(←)/Q(→) 키로 내 배지 좌우 이동
4. 하단 라인 도달 시 충돌 판정 → 피격/회피 처리
5. 온스크린 P/Q 패드 입력

## 특이사항
- 16:1515와 내용 차이 없음. 상세 분석은 16-1515.md 참조.
- 같은 이름 프레임의 페이지 간 복제(캔버스 y 11106 vs 7341). 구현 시 단일 화면으로 취급.
